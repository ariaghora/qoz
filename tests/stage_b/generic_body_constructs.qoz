// expect: 9
// Generic fn body builds a generic record literal with its own T.

type Wrapper<T> = { value: T }

let wrap<T>(x: T): Wrapper<T> = Wrapper { value: x }

let main(): i64 = {
    let w: Wrapper<i64> = wrap(9)
    w.value
}
