// expect: 42
// Compound assignment operators on primitives.

let main(): i64 = {
    var x: i64 = 10
    x += 5      // 15
    x -= 3      // 12
    x *= 4      // 48
    x /= 8      // 6
    x %= 5      // 1
    x += 41     // 42
    x
}
