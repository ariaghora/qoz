import std/fmt

#link("c")
#link_framework("CoreFoundation")

@link_name("puts")
external let print_cstr(s: cstring): i32

@link_name("qoz_string_eq")
external let str_eq(a: string, b: string): bool

@operator("==")
let my_eq(a: *Point, b: *Point): bool = a.x == b.x && a.y == b.y

type Point = { x: i64, y: i64 }

let main() = {
    fmt.println("hi")
}
