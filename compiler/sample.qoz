import std/fmt

let main() = {
    var i: i64 = 0
    var total: i64 = 0
    while i < 5 {
        total = total + i
        i = i + 1
    }
    if total > 10 {
        fmt.println("big", total)
    } else {
        fmt.println("small", total)
    }
}
