import std/fmt
import std/strings as s

type Point = { x: i64, y: i64 }
type Pair<A, B> = { fst: A, snd: B }

let answer = 42
let pi = 314
let total = answer + pi * 2

let add(x: i64, y: i64): i64 = x + y
let identity<T>(x: T): T = x

let main() = {
    let p = answer
    var q = total
    fmt.println("hello", p, q)
}
